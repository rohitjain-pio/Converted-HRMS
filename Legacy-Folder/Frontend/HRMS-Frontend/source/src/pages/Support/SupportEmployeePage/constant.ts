import { EmployeeFeedbackTypeFilter } from "@/services/Support/types";

export const DEFAULT_EMPLOYEE_FEEDBACK_TYPE_FILTERS: EmployeeFeedbackTypeFilter={
    feedbackType:0,
    ticketStatus:0,
    searchQuery:"",
    createdOnFrom:null,
   createdOnTo:null
}